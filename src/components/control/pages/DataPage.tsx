import React from 'react'
import { DataPropagationProps } from '../../../types/DataPropagation'

export const DataPage = ({
  data,
  onSave,
}: DataPropagationProps): JSX.Element => <div className="control-area-content">DataPage</div>
